# ==================================================
# Supabase Configuration
# ==================================================
# Astro SSR uses BOTH client-side and server-side code!
#
# PUBLIC_* = Available in browser (React components)
# Without PUBLIC_* = Server-side only (API routes, middleware)

# Client-side (React components need these)
PUBLIC_SUPABASE_URL=###
PUBLIC_SUPABASE_ANON_KEY=###

# Server-side (API routes, middleware - SAME values as above!)
SUPABASE_URL=###
SUPABASE_ANON_KEY=###

# Admin operations only (server-side, use with caution!)
SUPABASE_SERVICE_ROLE_KEY=###

# AI Receipt Processing (server-side only)
OPENROUTER_API_KEY=###
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# ==================================================
# Feature Flags Configuration
# ==================================================
# Environment name for feature flags (local, integration, prod)
# Use PUBLIC_ENV_NAME for client-side access (React components)
# Use ENV_NAME for server-side access (API routes, Astro pages)
PUBLIC_ENV_NAME=local
ENV_NAME=local

# ==================================================
# Testing Configuration
# ==================================================
# For E2E tests (uses remote Supabase)
# Copy to .env.test for E2E testing
E2E_USERNAME=test@example.com
E2E_PASSWORD=testpassword123
E2E_USERNAME_ID=00000000-0000-0000-0000-000000000000

# For Integration tests (uses local Supabase)
# Copy to .env.integration for integration testing
# Start local Supabase with: supabase start
# Local Supabase URLs:
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0